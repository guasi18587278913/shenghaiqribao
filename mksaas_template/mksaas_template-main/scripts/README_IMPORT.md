# 🚀 星球用户导入 - 快速指南

## 一、准备数据文件

### 方式 1：JSON 格式（推荐）

编辑 `scripts/planet-users.json`：

```json
{
  "users": [
    {
      "name": "张三",
      "phone": "13800138000",
      "planetNumber": "12345678"
    },
    {
      "name": "李四",
      "phone": "13900139000",
      "planetNumber": "87654321"
    }
  ]
}
```

### 方式 2：CSV 格式

创建 CSV 文件（例如 `my-users.csv`）：

```csv
name,phone,planetNumber
张三,13800138000,12345678
李四,13900139000,87654321
王五,15800158000,11223344
```

---

## 二、导入命令

```bash
# JSON 格式导入（使用默认文件）
pnpm tsx scripts/import-planet-users.ts

# JSON 格式导入（自定义文件）
pnpm tsx scripts/import-planet-users.ts your-file.json

# CSV 格式导入
pnpm tsx scripts/import-planet-users.ts your-file.csv
```

---

## 三、登录测试

1. 启动开发服务器：
   ```bash
   pnpm dev
   ```

2. 访问登录页面：
   ```
   http://localhost:3000/auth/planet-login
   ```

3. 使用导入的手机号和星球编号登录

---

## 四、数据字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | 文本 | 是 | 用户姓名 |
| phone | 文本 | 是 | 11位手机号（1开头） |
| planetNumber | 文本 | 是 | 星球编号 |
| role | 文本 | 否 | user 或 admin（默认 user） |

---

## 五、常见问题

### 手机号格式要求
- 必须是 11 位数字
- 必须以 1 开头
- 第二位必须是 3-9

### 重复处理
- 相同手机号会自动跳过，不会重复导入

### 查看已导入用户
```bash
# 打开 Drizzle Studio
pnpm db:studio

# 访问浏览器查看
https://local.drizzle.studio
```

---

## 📞 需要帮助？

查看完整文档：`PLANET_LOGIN_GUIDE.md`
